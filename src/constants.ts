export const PRIMITIVES = <const>{
  string: String,
  number: Number,
  bigint: BigInt,
  boolean: Boolean,
  symbol: Symbol,
  object: Object,
  array: Array,
  map: Map,
  date: Date,
  function: Function,
  null: null,
  undefined,
}
